{
  "name": "muyang-tools-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "build:live-sticker": "rm -rf dist && VITE_DEPLOY_BASE=/live-sticker/ VITE_BUILD_OUT_DIR=dist/live-sticker npm run build && node scripts/create-task-map-entry.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@fontsource/inter": "^5.2.8",
    "@vitejs/plugin-react": "latest",
    "@xyflow/react": "^12.11.1",
    "react": "latest",
    "react-dom": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "typescript": "latest"
  }
}
